import { GulpTask } from 'gulp-core-build';
import gulp = require('gulp');
export interface IKarmaTaskConfig {
    karmaConfigPath: string;
}
export declare class KarmaTask extends GulpTask<IKarmaTaskConfig> {
    name: string;
    taskConfig: IKarmaTaskConfig;
    resources: {
        bindPolyfillPath: string;
        istanbulInstrumenterLoaderPath: string;
        plugins: any[];
    };
    executeTask(gulp: gulp.Gulp, completeCallback: (result?: any) => void): void;
}
