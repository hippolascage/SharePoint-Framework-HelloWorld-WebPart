import { GulpTask } from './GulpTask';
import gulp = require('gulp');
export interface INukeConfig {
}
export declare class NukeTask extends GulpTask<INukeConfig> {
    name: string;
    taskConfig: INukeConfig;
    executeTask(gulp: gulp.Gulp, completeCallback: (result?: any) => void): Promise<any> | NodeJS.ReadWriteStream | void;
}
