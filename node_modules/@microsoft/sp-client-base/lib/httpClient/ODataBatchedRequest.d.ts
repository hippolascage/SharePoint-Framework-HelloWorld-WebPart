/**
 * @copyright (c) Microsoft Corporation. All rights reserved.
 */
export declare type ODataBatchedRequestResolveCallback = (value?: Response) => void;
export declare type ODataBatchedRequestRejectCallback = (value?: any) => void;
/**
 * INTERNAL USAGE ONLY
 * This is a helper class used by ODataBatch to track the state of each
 * queued REST request.
 */
export default class ODataBatchedRequest {
    request: Request;
    response: Response;
    promise: Promise<Response>;
    private _queuedPromiseResolves;
    private _queuedPromiseRejects;
    constructor(request: Request);
    /**
     * The ODataBatch class calls this method while parsing the HTTP response for
     * the batch.  If there are any queued promises, this will resolve or reject
     * them as appropriate.
     *
     * NOTE: This function is only ever called once, because a BATCH can only
     * be executed once.
     */
    notifyCompleted(response: Response): void;
}
