import * as React from 'react';
import { ICanvasContext } from '../canvasComponent/ICanvasComponent';
import { ICanvasZoneProps } from './ICanvasZone';
import './CanvasZone.scss';
export default class CanvasZone extends React.Component<ICanvasZoneProps, void> {
    static contextTypes: React.ValidationMap<CanvasZone>;
    /**
     * @ readOnly
     */
    private static _VERTICAL_MARGIN;
    context: ICanvasContext;
    private _childControl;
    private _childControlElement;
    private _domElement;
    private _toolbarElement;
    private _trapDiv;
    constructor(props: ICanvasZoneProps, context: ICanvasContext);
    shouldComponentUpdate(nextProps: ICanvasZoneProps): boolean;
    componentDidMount(): void;
    componentDidUpdate(prevProps: ICanvasZoneProps): void;
    render(): React.ReactElement<ICanvasZoneProps>;
    private _handleControlFocus(e);
    private _handleClickCapture(e);
    private _handleKeydownCapture(evt);
    private _handleToolbarKeydown(evt);
    private _handleMouseOut(e);
    private _handleMouseOver(e);
    private _handleTrapFocus(e);
    private _selectControl();
}
