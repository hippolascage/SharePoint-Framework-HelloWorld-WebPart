import * as React from 'react';
import { ISerializedCanvasControl } from '../../canvas/canvasControl/ISerializedCanvasControl';
export interface IToolboxItem extends React.Props<React.StatelessComponent<IToolboxItemProps>> {
    description: string;
    displayName: string;
    itemProps: ISerializedCanvasControl;
    msIconName: string;
}
export interface IToolboxItemProps extends IToolboxItem {
    onClick: (itemProps: ISerializedCanvasControl) => void;
}
