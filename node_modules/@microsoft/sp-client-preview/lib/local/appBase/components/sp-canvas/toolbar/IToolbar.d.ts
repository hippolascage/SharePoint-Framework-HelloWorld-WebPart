import { DisplayMode } from '@microsoft/sp-client-base';
import * as React from 'react';
import CanvasControlType from '../../canvas/canvas/CanvasControlType';
import Toolbar from './Toolbar';
export interface IToolbarProps extends React.Props<Toolbar> {
    controlType: CanvasControlType;
    displayMode: DisplayMode;
    id: string;
    row: number;
    onDeleteButtonClick: (row: number) => void;
    onConfigureButtonClick: (id: string) => void;
}
