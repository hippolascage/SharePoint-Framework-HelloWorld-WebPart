import * as React from 'react';
import { DisplayMode } from '@microsoft/sp-client-base';
export interface ICanvasZoneProps extends React.Props<React.StatelessComponent<ICanvasZoneProps>> {
    displayMode: DisplayMode;
    id: string;
    isSelected: boolean;
    row: number;
}
