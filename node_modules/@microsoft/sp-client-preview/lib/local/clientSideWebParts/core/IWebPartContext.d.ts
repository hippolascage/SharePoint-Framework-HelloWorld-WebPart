/**
 * @Copyright (c) Microsoft Corporation.  All rights reserved.
 *
 * Web part context intreface. The web part context is an object that
 */
import { IClientSideWebPartManifestInstance } from '@microsoft/sp-module-interfaces';
import { BasicHttpClient, Environment, HttpClient, PageContext, ServiceScope } from '@microsoft/sp-client-base';
import IClientSideComponentContext from './IClientSideComponentContext';
import IClientSideWebPartStatusRenderer from './../../common/webParts/IClientSideWebPartStatusRenderer';
import { IEventAggregator } from '../../common/events/IEvent';
import IWebPartHost from '../../common/webParts/IWebPartHost';
import { PropertyPaneState } from '../../common/webParts/PropertyPaneDefinitions';
/**
 * Web part context interface.
 */
interface IWebPartContext extends IClientSideComponentContext<IClientSideWebPartManifestInstance<any>> {
    /**
     * Web part instance id. This is a globally unique value.
     */
    instanceId: string;
    /**
     * Web part tag to be used for logging and telemetry.
     */
    webPartTag: string;
    /**
     * HttpClient instance scoped to this web part.
     */
    httpClient: HttpClient;
    /**
     * BasicHttpClient instance scoped to this web part.
     */
    basicHttpClient: BasicHttpClient;
    /**
     * SharePoint page context.
     */
    pageContext: PageContext;
    /**
     * Web part status renderer.
     */
    statusRenderer: IClientSideWebPartStatusRenderer;
    /**
     * Web part event aggregator.
     *
     * @internal
     */
    eventAggregator: IEventAggregator;
    /**
     * Function to open the PropertyPane to help configure this web part.
     */
    configureStart: (id: string, propertyPaneState?: PropertyPaneState) => void;
    /**
     * Web part host.
     */
    host: IWebPartHost;
    /**
     * Service scope instance that is scoped to this particular web part.
     */
    serviceScope: ServiceScope;
    /**
     * Environment that webparts are hosted in.
     */
    environment: Environment;
}
export default IWebPartContext;
