import { TSLintTask } from 'gulp-core-build-typescript/lib/TSLintTask';
import { GulpTask } from 'gulp-core-build';
export interface ITSLintPartialConfig {
    rulesDirectory: string | string[];
    rules: {
        [ruleName: string]: any;
    };
}
export declare function initializeTslintTask(tslintTask: TSLintTask | GulpTask<{}>): void;
