import { GulpTask } from 'gulp-core-build';
import gulp = require('gulp');
export interface ISassTaskConfig {
    sassMatch?: string[];
    useCSSModules?: boolean;
    dropCssFiles?: boolean;
}
export declare class SassTask extends GulpTask<ISassTaskConfig> {
    name: string;
    taskConfig: ISassTaskConfig;
    nukeMatch: string[];
    executeTask(gulp: gulp.Gulp, completeCallback?: (result?: string) => void): Promise<any> | NodeJS.ReadWriteStream | void;
    private _processFiles(gulp, srcPattern, completeCallback, postCSSPlugins);
    private _generateModuleStub(cssFileName, json);
    private generateScopedName(name, fileName);
}
