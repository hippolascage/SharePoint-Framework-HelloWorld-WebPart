import gulp = require('gulp');
export declare class GulpProxy {
    private _gulp;
    constructor(gulpInstance: any);
    task(): any;
    src(glob: string | string[], opt?: gulp.SrcOptions): NodeJS.ReadWriteStream;
    dest(outFolder: string | ((file: string) => string), opt?: gulp.DestOptions): NodeJS.ReadWriteStream;
    watch(glob: string | string[], fn: (gulp.WatchCallback | string)): NodeJS.EventEmitter;
}
