import * as React from 'react';
import { IGroupFooterProps, IGroup } from './GroupedList.Props';
import './GroupFooter.scss';
export interface IGroupFooter {
    group: IGroup;
    groupIndex: number;
    groupLevel: number;
    footerProps?: IGroupFooterProps;
}
export declare class GroupFooter extends React.Component<IGroupFooter, {}> {
    constructor(props: IGroupFooter);
    render(): JSX.Element;
    private _onToggleSummarize();
}
