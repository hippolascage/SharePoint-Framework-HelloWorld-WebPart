import * as React from 'react';
import './Nav.scss';
import { INavProps } from './Nav.Props';
export interface INavState {
    isGroupExpanded: boolean[];
}
export declare class Nav extends React.Component<INavProps, INavState> {
    static defaultProps: INavProps;
    constructor();
    render(): React.ReactElement<{}>;
    private _renderLink(link, linkIndex);
    private _renderLinks(links);
    private _renderGroup(group, groupIndex);
    private _onGroupHeaderClicked(groupIndex, ev);
}
